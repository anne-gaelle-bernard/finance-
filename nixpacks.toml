# Nixpacks Plan for Backend Only

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd backend && npm ci --production"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd backend && npm start"
